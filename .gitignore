**/target/
/.flattened-pom.xml
.claude
/.claude

# Eclipse project files
.classpath
.project
.settings/

# VS Code project files
.vscode/

# Local build artifacts
build_clean/
**/build_clean/
classpath.txt
**/classpath.txt
sources.txt
**/sources.txt

# MiRoVA cluster: generated demand inputs and run artifacts.
# Demand CSVs are regenerated from field data by the offline Python pipeline and are
# too large / too derived to track; the cluster workflow expects them under cluster/demand.
cluster/demand/
cluster/cp.txt
cluster/run_list.txt
cluster/logs/
cluster/output/

# Profiling artefacts: JFR recordings, raw jfr-print exports and profiling matrix run output.
# Individual files run past a gigabyte and every one of them is reproducible from a run, so
# none of it belongs in the repository. The findings live in docs/mirova/ instead.
profiling/
*.jfr
