<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.opentrafficsim</groupId>
    <artifactId>ots</artifactId>
    <version>1.7.6</version>
  </parent>

  <artifactId>ots-demo</artifactId>
  <name>OpenTrafficSim demo classes</name>
  <description>Demo classes for the OpenTrafficSim project</description>

  <dependencies>
  
    <dependency>
      <groupId>org.opentrafficsim</groupId>
      <artifactId>ots-xml</artifactId>
    </dependency> 
    
    <dependency>
      <groupId>org.opentrafficsim</groupId>
      <artifactId>ots-swing</artifactId>
    </dependency>

    <dependency>
      <groupId>jakarta.xml.bind</groupId>
      <artifactId>jakarta.xml.bind-api</artifactId>
    </dependency>

    <dependency>
      <groupId>org.glassfish.jaxb</groupId>
      <artifactId>jaxb-runtime</artifactId>
    </dependency>
    
  </dependencies>

  <repositories>
    <repository>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
      <id>osgeo</id>
      <name>osgeo</name>
      <url>https://repo.osgeo.org/repository/release/</url>
    </repository>
  </repositories>

  <profiles>
    <!--
      TaMA on the runtime classpath, so that a scenario can select it with the parameter tacticalPlanner=tama
      (see TacticalPlannerProvider). Runtime scope only: nothing in OTS compiles against TaMA, and without the profile
      the build is unchanged. The bundle is self-contained and comes from mavenLocal, published from the TaMA
      repository with ./gradlew :tama-ots:publishToMavenLocal.
    -->
    <profile>
      <id>tama</id>
      <dependencies>
        <dependency>
          <groupId>edu.kit.ifv.tama</groupId>
          <artifactId>tama-ots-bundle</artifactId>
          <version>0.1.0-SNAPSHOT</version>
          <scope>runtime</scope>
        </dependency>
      </dependencies>
    </profile>
  </profiles>

</project>